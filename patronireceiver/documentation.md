[comment]: <> (Code generated by mdatagen. DO NOT EDIT.)

# patronireceiver

## Default Metrics

The following metrics are emitted by default. Each of them can be disabled by applying the following configuration:

```yaml
metrics:
  <metric_name>:
    enabled: false
```

### patroni.cluster.unlocked

Value is 1 if the cluster is unlocked (no leader lock held), 0 if locked.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.dcs_last_seen

Epoch timestamp when DCS was last contacted successfully by Patroni.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| s | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.failsafe_mode.active

Value is 1 if DCS failsafe mode is currently active, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.history.lsn

WAL location (in bytes) at the moment the given timeline was promoted, from the cluster history entry. One data point per historical timeline.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| By | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |
| patroni.history.timeline | The PostgreSQL timeline number of a history entry (a promotion / timeline switch event). | Any Int | Recommended | - |
| patroni.history.reason | The reason recorded for the timeline switch (e.g. `no recovery target specified`). | Any Str | Recommended | - |

### patroni.history.promoted_at

Epoch timestamp (seconds) when the given timeline was created (promoted), parsed from the cluster history entry. One data point per historical timeline.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| s | Gauge | Double | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |
| patroni.history.timeline | The PostgreSQL timeline number of a history entry (a promotion / timeline switch event). | Any Int | Recommended | - |
| patroni.history.reason | The reason recorded for the timeline switch (e.g. `no recovery target specified`). | Any Str | Recommended | - |

### patroni.history.size

Total number of timeline switch (promotion) entries recorded in the cluster history.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.is_paused

Value is 1 if auto failover is disabled (maintenance/pause mode), 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.pending_restart

Value is 1 if the node needs a restart, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.postgres.in_archive_recovery

Value is 1 if PostgreSQL is replicating from archive, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.postgres.running

Value is 1 if PostgreSQL is running, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |
| patroni.role | The role of the node, e.g. `primary`, `replica`, `standby_leader`. | Any Str | Recommended | - |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |

### patroni.postgres.server_version

PostgreSQL server version (if running), 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.postgres.state

Numeric representation of the PostgreSQL state. 0=initdb, 1=initdb_failed, 2=custom_bootstrap, 3=custom_bootstrap_failed, 4=creating_replica, 5=running, 6=starting, 7=bootstrap_starting, 8=start_failed, 9=restarting, 10=restart_failed, 11=stopping, 12=stopped, 13=stop_failed, 14=crashed. Numeric values are fixed and never change for backward compatibility.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |
| patroni.role | The role of the node, e.g. `primary`, `replica`, `standby_leader`. | Any Str | Recommended | - |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.state | The textual PostgreSQL state of the node, e.g. `running`, `starting`. | Any Str | Recommended | - |

### patroni.postgres.streaming

Value is 1 if PostgreSQL is streaming, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.postgres.timeline

PostgreSQL timeline of this node (if running), 0 otherwise.

| Unit | Metric Type | Value Type | Aggregation Temporality | Monotonic | Stability |
| ---- | ----------- | ---------- | ----------------------- | --------- | --------- |
| 1 | Sum | Int | Cumulative | true | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.postmaster_start_time

Epoch seconds since the PostgreSQL postmaster started.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| s | Gauge | Double | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.primary

Value is 1 if this node is the leader (primary), 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.quorum_standby

Value is 1 if this node is a quorum standby replica, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.replica

Value is 1 if this node is a replica, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.replication.lag

Total replication lag in bytes between the standby and the leader.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| By | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |
| patroni.replication.application_name | The `application_name` of the replication standby as seen by the primary. | Any Str | Recommended | - |
| patroni.replication.client_addr | The client IP address of the replication standby. | Any Str | Recommended | - |
| patroni.replication.sync_state | The synchronous state of the standby, e.g. `async`, `sync`, `quorum`, `potential`. | Any Str | Recommended | - |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |

### patroni.replication.receive_lag

Receive lag in bytes between the leader and the standby.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| By | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |
| patroni.replication.application_name | The `application_name` of the replication standby as seen by the primary. | Any Str | Recommended | - |
| patroni.replication.client_addr | The client IP address of the replication standby. | Any Str | Recommended | - |
| patroni.replication.sync_state | The synchronous state of the standby, e.g. `async`, `sync`, `quorum`, `potential`. | Any Str | Recommended | - |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |

### patroni.replication.replay_lag

Replay lag in bytes between received and replayed WAL on the standby.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| By | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |
| patroni.replication.application_name | The `application_name` of the replication standby as seen by the primary. | Any Str | Recommended | - |
| patroni.replication.client_addr | The client IP address of the replication standby. | Any Str | Recommended | - |
| patroni.replication.sync_state | The synchronous state of the standby, e.g. `async`, `sync`, `quorum`, `potential`. | Any Str | Recommended | - |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |

### patroni.standby_leader

Value is 1 if this node is the standby_leader, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.sync_standby

Value is 1 if this node is a synchronous standby replica, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.version

Patroni semver rendered without periods (e.g. `040000` for `4.0.0`).

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.version | The Patroni version string (e.g. `4.0.0`). | Any Str | Recommended | - |

### patroni.xlog.location

Current location of the PostgreSQL transaction log (WAL), 0 if this node is not the leader.

| Unit | Metric Type | Value Type | Aggregation Temporality | Monotonic | Stability |
| ---- | ----------- | ---------- | ----------------------- | --------- | --------- |
| By | Sum | Int | Cumulative | true | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.xlog.paused

Value is 1 if the PostgreSQL xlog is paused, 0 otherwise.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.xlog.received_location

Current location of the received PostgreSQL transaction log, 0 if this node is not a replica.

| Unit | Metric Type | Value Type | Aggregation Temporality | Monotonic | Stability |
| ---- | ----------- | ---------- | ----------------------- | --------- | --------- |
| By | Sum | Int | Cumulative | true | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.xlog.replayed_location

Current location of the replayed PostgreSQL transaction log, 0 if this node is not a replica.

| Unit | Metric Type | Value Type | Aggregation Temporality | Monotonic | Stability |
| ---- | ----------- | ---------- | ----------------------- | --------- | --------- |
| By | Sum | Int | Cumulative | true | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |

### patroni.xlog.replayed_timestamp

Epoch timestamp of the replayed PostgreSQL transaction log, 0 if null.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| s | Gauge | Double | Development |

#### Attributes

| Name | Description | Values | Requirement Level | Semantic Convention |
| ---- | ----------- | ------ | ----------------- | ------------------- |
| patroni.scope | The cluster scope (name) this Patroni node belongs to (`patroni.scope`). | Any Str | Recommended | - |
| patroni.node.name | The name of the Patroni node (`patroni.name`). | Any Str | Recommended | - |
